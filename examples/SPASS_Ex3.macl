list_of_formulae(axioms)

	C(admin, df1).
	[](admin, C(bob, df1)).
	//[](bob, alice => bob).
	[](bob, ([](alice, df1) -> [](bob, df1))).
	[](alice, df1).

end_of_list

list_of_formulae(conjectures)

	df1.

end_of_list
